# fill-nerve-sim
A container-based simulation of Nerve API for the FILL use case

# To start
```bash docker compose up --build```

The backend API will be available at ```http://localhost:3000/```

And the frontend is at: ```http://localhost:8000```